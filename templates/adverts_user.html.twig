{% extends 'base.html.twig' %}

{% block title %}Mes Favoris{% endblock %}

{% block body %}
    <h1 class="favoris">{{ title }}</h1>

        {% for advert in adverts %}
            <div class="annonce_favorite">
            <a class=titre_annonce href="/advert/{{ advert.getId() }}"><h2>{{ advert.getAdvertTitle() }}</h2></a>
            <h3 class="prix">{{ advert.getAdvertPrice() }} €</h3>
            <h6>{{ advert.getAdvertCategory() }}</h6>
            <h6>{{ advert.getAdvertLocalisation() }}</h6>
            <p>{{ advert.getAdvertDate()|date }}</p>
            </div>

        {% endfor %}


{% endblock %}